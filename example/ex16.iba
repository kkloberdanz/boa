collatz = n => {
    printd(n)
    if n == 1:
        then 1
    else
        if n % 2 == 0:
            then collatz(n / 2)
        else
            then collatz(3 * n + 1)
}

make_even = n => {
    if n % 2 == 0: then n else then n + 1
}

collatz(1000)
printd(make_even(1111))
