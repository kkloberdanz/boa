def my_func(my_var) {
    y := if my_var > 3 {
        then 5
    } else {
        then 6
    }

    then y + 10
}

(Integer) -> List(Integer)
def lazy(max_n) {
    acc := 1
    prev := 0
    for i, num in enumerate(range(max_n)) {
        if i <= max_n {
            yield acc
            next_n := prev + acc
            prev := acc
            acc := next_n
        } else {
            break
        }
    }
}

def main(argv) {
    for i in lazy(100) {
        print(i)
    }
}
