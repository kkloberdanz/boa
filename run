#!/bin/bash

PROG_NAME=$1

set -e

if [[ -z $CC ]]; then
    CC=gcc
fi

echo "CC=${CC}"

./boa example/$PROG_NAME.boa

$CC -Os -Wall -Wextra -Wpedantic -Werror \
    example/$PROG_NAME.c libruntime.a -o bin/$PROG_NAME

./bin/$PROG_NAME
